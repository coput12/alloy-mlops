PidFile=/tmp/zabbix_agentd.pid
LogFile=/tmp/zabbix_agentd.log
Server=zabbix-server
ServerActive=zabbix-server
Hostname=alloy-mlops-host
Timeout=3
RefreshActiveChecks=120

# Данные о хосте через скрипты/статьи: тут минимум — примеры контролей
# UnsafeUserParameters=1
# UserParameter=ml_health,curl -s http://ml-service:8000/health | python3 -c "import sys,json;print(1 if json.load(sys.stdin)['status']=='ok' else 0)"